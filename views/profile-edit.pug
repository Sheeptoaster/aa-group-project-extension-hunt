extends layout

include utils.pug

block content
    h1 Edit Page
    form(action='/profiles/:id/edit', method='POST')
        input(type='hidden' name='_METHOD' value='PATCH')
        input(type='hidden' name='_csrf' value=csrfToken)
        
